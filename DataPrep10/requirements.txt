# =============================================================================
# PhishURL Detection Pipeline — data_prep8
# GPU-Ready Requirements (CUDA 11.8)
# =============================================================================
# Install everything in one command:
#   pip install -r requirements.txt
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cu118

# --- PyTorch (GPU — CUDA 11.8) ---
torch
torchvision
torchaudio
torchinfo>=1.8.0

# --- HuggingFace & ONNX ---
transformers>=4.38.0
peft>=0.11.0
accelerate>=0.27.0
optimum>=1.19.0
onnx>=1.15.0
onnxscript>=0.7.0
onnxruntime>=1.17.0

# --- Data Processing & ML ---
pandas>=1.5.0
numpy>=1.23,<3
scikit-learn>=1.0.0

# --- Visualization ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Utilities ---
pyyaml>=6.0.0
mlflow>=2.10.0
tqdm>=4.65.0
requests>=2.31.0
packaging
python-dateutil>=2.8.0

# --- URL Parsing (required by preprocessing) ---
tldextract>=3.4.0
idna>=3.4

# --- Quantization Toolkits ---
neural-compressor>=2.4.1     # Intel Neural Compressor (for INT4 ONNX)
bitsandbytes>=0.43.0         # 4-bit / 8-bit GPU quantization
